# PBY Queue App

A simple queue app for managing large groups of people waiting for photos: import contacts, place parties in line, call the next party, and notify them by text when they're "Up next" or it's their turn.

See `docs/` for product decisions, feature priorities, and design.
